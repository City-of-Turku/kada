This file describes the feature, from developer to developer. It should focus on why something has been implemented instead of how, but some tricky or special implementations should be mentioned here. Especially if they cannot be documented in code.

== PURPOSE ==
This features holds all domain configuration. Client needs different types of branded pages, which might get a top-level domain or sub-domain.

== CHANGELOG ==
* 2015-02-04 *
#601: Removed content visibility variables, because they are always overriden when a domain is added.

* 2015-01-22 *
Brands taxonomy and field + domain configuration for branded sites

* 2015-01-20 *
#673: Include domain module in the build and create a feature for configuration.
